CREATE TABLE public.employee_photo
(
    employee_id 	INT NOT NULL,
    photo_format 	varchar(10) NOT NULL,
    photo			BYTEA NOT NULL,
    sender_id		INT,
    PRIMARY KEY (employee_id, photo_format)
);

ALTER TABLE public.employee_photo
ADD CONSTRAINT fk_emp_id
FOREIGN KEY (employee_id)
REFERENCES public.employee(id)
ON DELETE RESTRICT;

ALTER TABLE public.employee_photo
ADD CONSTRAINT fk_sender_id
FOREIGN KEY (sender_id)
REFERENCES public.employee(id)
ON DELETE SET NULL;